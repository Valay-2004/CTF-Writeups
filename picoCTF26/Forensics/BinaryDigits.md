


We are give a file named `digits.bin`
```bash
└─$ file digits.bin 
digits.bin: ASCII text, with very long lines (65536), with no line terminators
```

Thus it's an ASCII text file with no line terminators.

So when checked it on `CyberChef` [Link](https://gchq.github.io/CyberChef/#recipe=From_Binary('None',8)Render_Image('Raw')) We can see it's an image of the flag so we save the image or send it to google lens to get the flag 

![[binaryToImage.excalidraw|2023]]
which is > `picoCTF{h1dd3n_1n_th3_b1n4ry_cc2099d3}`


